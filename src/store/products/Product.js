export class Product {
  name;
  quantity;
  price;

  constructor(name,
              quantity,
              price) {
    this.name     = name;
    this.quantity = quantity;
    this.price    = price;
  }
}
